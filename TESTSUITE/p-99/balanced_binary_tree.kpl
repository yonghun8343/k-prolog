균형잡힌_이진트리(0, 빈) :- !.
균형잡힌_이진트리(_엔, 노드(x, _엘, _알)) :- 
    _엔 > 0,
    _엔0 := _엔 - 1,
    _엔1 := _엔0 // 2,
    _엔2 := _엔0 - _엔1,
    분배(_엔1, _엔2, _엔엘, _엔알),
    균형잡힌_이진트리(_엔엘, _엘),
    균형잡힌_이진트리(_엔알, _알).

분배(_엔, _엔, _엔, _엔) :- !.
분배(_엔1, _엔2, _엔1, _엔2).
분배(_엔1, _엔2, _엔2, _엔1).

?- 균형잡힌_이진트리(4,T).